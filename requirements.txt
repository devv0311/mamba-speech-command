# Core
torch>=2.2.0          # PyTorch with MPS (Apple Silicon) support built in since 2.0+
torchaudio>=2.2.0      # used for tensor-level transforms (Resample, MelSpectrogram)
soundfile>=0.12        # used for actual WAV file I/O (torchaudio's own load/save now
                        # require the heavier optional `torchcodec` package in recent
                        # releases; soundfile is lighter and has no such dependency —
                        # see src/audio/io.py docstring for details)
numpy>=1.24
scipy>=1.11

# Data / config / experiment tracking
pyyaml>=6.0
pandas>=2.0
tqdm>=4.66

# Metrics & figures
scikit-learn>=1.3
matplotlib>=3.7
seaborn>=0.13

# Real-time audio I/O for the microphone demo
sounddevice>=0.4.6

# Real-time UI
dearpygui>=1.10; python_version >= "3.10"  # lightweight, no Node/web stack needed

# Testing
pytest>=7.4

# NOTE (Apple Silicon / MPS constraint):
# The official CUDA-optimized `mamba-ssm` / `causal-conv1d` packages from the Mamba
# authors require CUDA and nvcc and CANNOT install or run on macOS/Apple Silicon.
# They are intentionally NOT listed here. This project implements the Selective-SSM
# recurrence directly in PyTorch (src/models/mamba.py), which runs on CPU and MPS.
# See paper/methodology_notes.md for the full justification.
